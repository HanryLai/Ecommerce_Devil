import { AccountEntity, DetailInformationEntity, RoleEntity } from './auth';
import { ProductEntity } from './ecommerce/index';

export const entities = [AccountEntity, DetailInformationEntity, RoleEntity, ProductEntity];
